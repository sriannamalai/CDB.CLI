<!-- generated by gendocs from the command registry; do not edit by hand -->

# tasks

List the tasks CouchDB is running right now

## Synopsis

```
cdb tasks [flags]
```

## Description

List the tasks CouchDB is running right now.

Every row is one entry of _active_tasks: a running replication, a database or
view compaction, or an index build. A task that has finished is gone from the
list; "tasks" is a snapshot, not a history.

--watch re-reads the list every --interval and prints the rows again whenever
the set of tasks changes. The rows are written as they arrive rather than as an
aligned table, because a table cannot be drawn until its last row is known and
a watch has no last row. Press Ctrl-C to stop.

## Arguments

Takes no arguments.

## Flags

| Flag | Type | Default | Description |
|---|---|---|---|
| `--db` | string |  | show only tasks for this database path |
| `--interval` | duration | `2s` | how often --watch re-reads the list |
| `--type` | string |  | show only tasks of this type (replication, database_compaction, view_compaction, indexer, search_indexer) |
| `--watch` | bool | `false` | keep the list up to date until interrupted |

## Global flags

| Flag | Type | Default | Description |
|---|---|---|---|
| `--anonymous` | bool | `false` | connect without credentials, and do not ask for any |
| `--color` | string |  | colour: auto, always or never |
| `--format` | string |  | output format: table or json |
| `--json` | bool | `false` | print raw JSON instead of a table |
| `--pager` | string |  | pager command, or off |
| `--path` | string |  | starting virtual path |
| `--profile` | string |  | connection profile to use, overriding CDB_PROFILE and the default |
| `--replication-url` | string |  | address the server should use to reach itself for replication, overriding the profile and CDB_REPLICATION_URL |
| `--url` | string |  | server URL, overriding the profile and CDB_URL |
| `--verbose` | bool | `false` | include raw status codes and reasons in errors |
| `--yes` | bool | `false` | skip confirmation prompts |

## Examples

```
$ cdb tasks
 TYPE                | PROGRESS | TARGET                 | STARTED  | UPDATED  | NODE
---------------------+----------+------------------------+----------+----------+---------------
 database_compaction | 84%      | movies                 | 15:20:00 | 15:20:31 | nonode@nohost

$ cdb tasks --type replication --watch
$ cdb tasks --db /movies
```
